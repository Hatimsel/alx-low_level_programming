#include "main.h"

/**
  * _abs(int) - Computes the absolute value of
  * an integer.
  *
  * @i: input number as an integer.
  *
  * Return: always 0 success.
  */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
